#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../why-is-node-running/cli.js" "$@"
else
  exec node  "$basedir/../../../why-is-node-running/cli.js" "$@"
fi
# cmd-shim-target=/home/user/mycode/oxideterm/node_modules/.pnpm/vitest@4.1.9_@types+node@22.20.0_jsdom@29.1.1_vite@7.3.5_@types+node@22.20.0_jiti@2.7.0_lightningcss@1.32.0_/node_modules/why-is-node-running/cli.js
