#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../typescript/bin/tsserver" "$@"
else
  exec node  "$basedir/../../../typescript/bin/tsserver" "$@"
fi
# cmd-shim-target=/home/user/mycode/oxideterm/node_modules/.pnpm/react-i18next@16.6.6_i18next@25.10.10_typescript@5.9.3__react-dom@19.2.7_react@19.2.7__react@19.2.7_typescript@5.9.3/node_modules/typescript/bin/tsserver
