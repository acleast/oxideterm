#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

exec "$basedir/../../../esbuild/bin/esbuild"   "$@"
exit $?
# cmd-shim-target=/home/user/mycode/oxideterm/node_modules/.pnpm/vite@7.3.5_@types+node@22.20.0_jiti@2.7.0_lightningcss@1.32.0/node_modules/esbuild/bin/esbuild
